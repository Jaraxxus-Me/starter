# PPO agent configuration for L-navigate environment
name: ppo

args:
  total_timesteps: 1000000
  num_envs: 8
  num_steps: 256
  hidden_size: 128
  async_envs: true

torch_deterministic: true
cuda: false

exp_name: "ppo_l_navigate"
tb_log_dir: "runs"
